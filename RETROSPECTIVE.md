# 📝 프로젝트 회고 (Project Retrospective)

이 문서는 **EZ.GG** 프로젝트를 개발하며 겪었던 기술적 도전, 트러블슈팅 과정을 기록한 공간입니다.

> **"이번 프로젝트에서 나의 일차적인 목표는 SPA로 구성된 프로젝트를 완성하는 것이었다."**

개발자가 되기로 결심한 후 국비지원 학원을 다니며 처음 진행한 프로젝트는 JSP와 Spring으로 구성된 SSR 방식이었습니다. 그 경험을 바탕으로 취업에 성공했지만, 업무와 병행하며 개인 프로젝트를 진행하기에는 어려움이 있었습니다. 한 단계 더 성장하기 위해 과감히 퇴사를 결심하고, 실무에 가까운 환경에서 부족한 점을 보완하고자 이번 프로젝트를 시작했습니다.

이번에는 **체계적인 협업과 프로세스**를 목표로 삼았습니다.
- **Git 전략**: 브랜치 전략 수립 및 PR 기반 코드 리뷰 시행
- **협업 툴**: Notion(진행 상황/에러/기능 명세), Discord(상시 소통) 적극 활용
- **아키텍처**: 원하던 REST API 기반 SPA 구조 설계 도전

<br>

## 🚀 1. 기술적 도전과 해결 (Technical Challenges & Solutions)

### 🔹 1. SPA 환경에서의 CORS와 WebSocket 이슈
#### • 문제 상황 (Problem)
REST API 기반 SPA 구조를 처음 설계하며 **CORS(Cross-Origin Resource Sharing)** 문제가 가장 큰 걸림돌이었습니다. 단순히 `CorsConfigurationSource` 설정으로 해결될 줄 알았으나, **WebSocket**과 **Spring Security**가 결합된 환경에서는 예상치 못한 CORS 에러가 반복적으로 발생했습니다.

#### • 깨달음 (Insight)
단순한 설정 변경을 넘어 아키텍처 관점에서의 해결책이 필요함을 느꼈습니다. 이 과정을 통해 다음 프로젝트에서는 **Nginx**나 **Caddy** 같은 **리버스 프록시(Reverse Proxy)**를 도입하여, 초기부터 Client와 Server를 **단일 도메인(Single Domain)** 구조로 구성하는 것이 근본적인 해결책이자 훨씬 효율적이라는 교훈을 얻었습니다.

<br>

### 🔹 2. Spring Security Filter와 인가 처리
#### • 문제 상황 (Problem)
Spring Security 설정에서 `requestMatchers(...).permitAll()`을 사용하면 해당 경로는 필터를 거치지 않을 것이라 오해했습니다. 하지만 실제로는 **인증만 통과할 뿐, 등록된 필터(Filter)들은 여전히 실행**된다는 점을 디버깅을 통해 알게 되었습니다. 이로 인해 인증이 필요 없는 API에서도 불필요한 토큰 검증 로직이 도는 문제가 있었습니다.

#### • 해결 과정 (Solution)
`OncePerRequestFilter`를 상속받은 커스텀 `JWTFilter` 내부에서 **`shouldNotFilter()`** 메서드를 오버라이딩하여 해결했습니다. 특정 경로(로그인, 회원가입 등)에 대해서는 아예 필터 로직이 실행되지 않도록 명시적으로 제외 처리(Skip)를 구현하여 문제를 깔끔하게 해결했습니다.


<br>

### 🔹 3. 실시간 매칭 시스템의 중복 매칭과 동시성
#### • 문제 상황 (Problem)
실시간 매칭 시스템을 구현하는 과정에서, **동시에 여러 요청이 들어올 때 발생하는 중복 매칭 문제**가 가장 큰 과제였습니다. 초기에는 Redis List 기반의 큐를 사용하여 단순히 요청을 처리했으나, 요청이 몰리는 상황에서 동일한 유저가 여러 번 매칭되는 치명적인 문제가 발생했습니다.

#### • 해결 과정 (Solution)
단순한 코드 최적화가 아닌 **구조적 개선**이 필요하다고 판단하여 매칭 큐를 **Redis Streams** 기반으로 전면 개편했습니다. 
- **Consumer 단일화**: 컨슈머를 단일화하여 매칭 요청을 순차적으로 처리하도록 변경했습니다.
- **즉시 상태 삭제**: 매칭된 두 유저는 Elasticsearch에서 즉시 삭제하여 매칭되지 않은 유저 풀(Pool)에서만 검색되도록 상태를 엄격히 관리했습니다.
- **재시도 큐(Retry Queue) 도입**: 적합한 상대를 찾지 못한 경우 ZSET 기반의 재시도 큐에 넣어 일정 시간 후 다시 매칭을 시도하도록 설계했습니다.

#### • 결과 및 배운 점 (Result & Insight)
이를 통해 속도보다는 **안정성**을 우선시하는 구조로 개선하여 중복 매칭 문제를 해결했고, 추후 다중 컨슈머를 도입해 병렬 처리가 가능한 확장성까지 확보했습니다. 

<br>
 

## 💭 마치며

이번 프로젝트에서는 처음 사용하는 기술도 많았고, 그렇기 때문에 완성한 지금 되돌아보면 부족한 점도 많습니다. 또 자세히 들여다보면 내가 인지하지 못한 잘못된 부분도 분명 있을 것입니다. 

하지만 처음부터 완벽하게 기능을 이해하고 구현하는 사람은 많지 않다고 생각합니다. 세상에는 내가 아직 경험하지 못한 기술이 너무 많고, 앞으로도 계속해서 새로운 기술이 등장할 것입니다. 

이번 프로젝트처럼 익숙하지 않아도 **도전하고, 직접 부딪히고, 하나씩 해결해 나가는 경험**을 반복하다 보면 분명 과거의 나보다 성장한 내 모습을 만나게 될 것이라고 믿습니다.
